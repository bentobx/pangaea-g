extends(src='layout.sgr')
  block(name='content')
    header
      h1 {{ item.postTitle }}

    section
      article
        .content {{{ md(item.postContent) }}}
        // p {{ JSON.stringify(item) }}
        // h2 {{ item.record.author.authorName }}
